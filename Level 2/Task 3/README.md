# Level 2 – Task 3: K-Means Clustering

Performed K-Means clustering on the Iris dataset by standardizing numerical features,
determining the optimal number of clusters using the Elbow Method, and visualizing
cluster groupings using scatter plots.
